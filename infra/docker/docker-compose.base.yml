# Base data plane for local development.
#   docker compose -f infra/docker/docker-compose.base.yml up -d
name: hermesell-base

services:
  postgres:
    image: postgres:16-alpine
    container_name: hermesell-postgres
    environment:
      POSTGRES_DB: hermesell
      POSTGRES_USER: hermesell
      POSTGRES_PASSWORD: hermesell
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hermesell"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: hermesell-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  pg_data:
  redis_data:
